/**
 * 장기요양기관 검색 API 프록시
 */

const https = require('https');

const API_HOST = 'apis.data.go.kr';
const SEARCH_PATH = '/B550928/searchLtcInsttService02/getLtcInsttSeachList02';
const PAGE_SIZE = 10;

// 건보공단 신 시도코드 (행정안전부 개편 후)
const SIDO_CODE = {
  '서울특별시': '11', '부산광역시': '26', '대구광역시': '27',
  '인천광역시': '28', '대전광역시': '30', '울산광역시': '31',
  '세종특별자치시': '36', '경기도': '41', '충청북도': '43',
  '충청남도': '44', '경상북도': '47', '경상남도': '48',
  '제주특별자치도': '50', '강원특별자치도': '51', '강원도': '51',
  '광주광역시': '29',
  '전북특별자치도': '52', '전라북도': '52',
  '전라남도': '46',
};

// 시도별 시군구코드 (법정동코드 시군구 3자리)
const SIGUNGU_BY_SIDO = {
  '서울특별시': {
    '강남구':'680','강동구':'740','강북구':'305','강서구':'500','관악구':'620',
    '광진구':'215','구로구':'530','금천구':'545','노원구':'350','도봉구':'320',
    '동대문구':'230','동작구':'590','마포구':'440','서대문구':'410','서초구':'650',
    '성동구':'200','성북구':'290','송파구':'710','양천구':'470','영등포구':'560',
    '용산구':'170','은평구':'380','종로구':'110','중구':'140','중랑구':'260',
  },
  '부산광역시': {
    '강서구':'120','금정구':'110','기장군':'710','남구':'070','동구':'030',
    '동래구':'060','부산진구':'050','북구':'080','사상구':'150','사하구':'100',
    '서구':'020','수영구':'140','연제구':'130','영도구':'040','중구':'010','해운대구':'090',
  },
  '대구광역시': {
    '군위군':'720','남구':'040','달서구':'070','달성군':'710','동구':'020',
    '북구':'050','서구':'030','수성구':'060','중구':'010',
  },
  '인천광역시': {
    '강화군':'710','계양구':'070','남동구':'050','동구':'020','미추홀구':'030',
    '부평구':'060','서구':'080','연수구':'040','옹진군':'720','중구':'010',
  },
  '광주광역시': {
    '광산구':'060','남구':'030','동구':'010','북구':'040','서구':'020',
  },
  '대전광역시': {
    '대덕구':'050','동구':'010','서구':'030','유성구':'040','중구':'020',
  },
  '울산광역시': {
    '남구':'020','동구':'030','북구':'040','울주군':'710','중구':'010',
  },
  '세종특별자치시': { '세종시':'010' },
  '경기도': {
    '가평군':'820','고양시 덕양구':'281','고양시 일산동구':'285','고양시 일산서구':'287',
    '과천시':'290','광명시':'210','광주시':'610','구리시':'310','군포시':'410',
    '김포시':'570','남양주시':'360','동두천시':'250','부천시':'190',
    '성남시 분당구':'135','성남시 수정구':'131','성남시 중원구':'133',
    '수원시 권선구':'113','수원시 영통구':'117','수원시 장안구':'111','수원시 팔달구':'115',
    '시흥시':'390','안산시 단원구':'273','안산시 상록구':'271','안성시':'550',
    '안양시 동안구':'173','안양시 만안구':'171','양주시':'630','양평군':'830',
    '여주시':'670','연천군':'800','오산시':'370',
    '용인시 기흥구':'463','용인시 수지구':'465','용인시 처인구':'461',
    '의왕시':'430','의정부시':'150','이천시':'500','파주시':'480','평택시':'220',
    '포천시':'650','하남시':'450','화성시':'590',
  },
  '강원특별자치도': {
    '강릉시':'050','고성군':'810','동해시':'070','삼척시':'100','속초시':'090',
    '양구군':'790','양양군':'820','영월군':'740','원주시':'030','인제군':'800',
    '정선군':'760','철원군':'770','춘천시':'010','태백시':'080','평창군':'750',
    '홍천군':'720','화천군':'780','횡성군':'730',
  },
  '충청북도': {
    '괴산군':'760','단양군':'800','보은군':'720','영동군':'740','옥천군':'730',
    '음성군':'770','제천시':'150','증평군':'745','진천군':'750',
    '청주시 상당구':'111','청주시 서원구':'112','청주시 청원구':'114','청주시 흥덕구':'113',
    '충주시':'130',
  },
  '충청남도': {
    '계룡시':'250','공주시':'150','금산군':'710','논산시':'230','당진시':'270',
    '보령시':'180','부여군':'760','서산시':'210','서천군':'770','아산시':'200',
    '예산군':'810','천안시 동남구':'131','천안시 서북구':'133','청양군':'790',
    '태안군':'825','홍성군':'800',
  },
  '전북특별자치도': {
    '고창군':'770','군산시':'130','김제시':'210','남원시':'190','무주군':'730',
    '부안군':'780','순창군':'760','완주군':'710','익산시':'140','임실군':'750',
    '장수군':'740','전주시 덕진구':'113','전주시 완산구':'111','정읍시':'180','진안군':'720',
  },
  '전라남도': {
    '강진군':'780','고흥군':'740','곡성군':'720','광양시':'090','구례군':'730',
    '나주시':'060','담양군':'710','목포시':'010','무안군':'810','보성군':'750',
    '순천시':'050','신안군':'890','여수시':'030','영광군':'830','영암군':'800',
    '완도군':'870','장성군':'840','장흥군':'770','진도군':'880','함평군':'820',
    '해남군':'790','화순군':'760',
  },
  '경상북도': {
    '경산시':'150','경주시':'030','고령군':'780','구미시':'060','군위군':'720',
    '김천시':'040','문경시':'120','봉화군':'840','상주시':'100','성주군':'790',
    '안동시':'050','영덕군':'760','영양군':'750','영주시':'080','영천시':'090',
    '예천군':'820','울릉군':'880','울진군':'850','의성군':'730','청도군':'770',
    '청송군':'740','칠곡군':'800','포항시 남구':'010','포항시 북구':'020',
  },
  '경상남도': {
    '거제시':'250','거창군':'800','고성군':'750','김해시':'220','남해군':'760',
    '밀양시':'240','사천시':'180','산청군':'780','양산시':'270','의령군':'720',
    '진주시':'155','창녕군':'740','창원시 마산합포구':'125','창원시 마산회원구':'127',
    '창원시 성산구':'120','창원시 의창구':'110','창원시 진해구':'130',
    '통영시':'170','하동군':'770','함안군':'730','함양군':'790','합천군':'810',
  },
  '제주특별자치도': {
    '서귀포시':'020','제주시':'010',
  },
};

// 기관유형코드 → 화면 배지 (facility-type.mjs SHORT 기준)
const ADMIN_PTTN = {
  A01:'요양원', A02:'전문요양원', A03:'요양원', A04:'공동생활가정',
  A05:'요양원', AAA:'입소시설',
  B01:'방문요양', B02:'방문목욕', B03:'주야간보호', B04:'단기보호', B05:'방문간호',
  C01:'방문요양', C02:'방문목욕', C03:'주야간보호', C04:'단기보호', C05:'방문간호',
  C06:'복지용구',
  S41:'치매전담 공동생활가정', Z01:'기타',
};
function facilityTypeName(code) {
  if (!code) return code || '';
  const k = String(code).trim().toUpperCase();
  if (ADMIN_PTTN[k]) return ADMIN_PTTN[k];
  // G/M/H/I + 31~99 범위 코드
  const m = /^([GMHI])(\d{2})$/.exec(k);
  if (m) {
    const n = Number(m[2]);
    if (n >= 31 && n <= 99) {
      if (m[1] === 'G') return '치매전담실(가형)';
      if (m[1] === 'M') return '치매전담실(나형)';
      return '주야간 치매전담';
    }
  }
  return k;
}

exports.handler = async function (event) {
  const headers = {
    'Access-Control-Allow-Origin': '*',
    'Content-Type': 'application/json; charset=utf-8',
  };

  if (event.httpMethod === 'OPTIONS') return { statusCode: 204, headers };

  const apiKey = process.env.LTC_API_KEY;
  if (!apiKey) {
    return { statusCode: 500, headers, body: JSON.stringify({ error: 'API 키가 설정되지 않았습니다.' }) };
  }

  const { sido = '', sigungu = '', pageIndex = '1' } = event.queryStringParameters || {};

  const siDoCd = SIDO_CODE[sido];
  if (!siDoCd) {
    return { statusCode: 400, headers, body: JSON.stringify({ error: `지원하지 않는 시도입니다: ${sido}` }) };
  }

  const siGunGuCd = (SIGUNGU_BY_SIDO[sido] || {})[sigungu];

  const params = new URLSearchParams({
    serviceKey: apiKey,
    pageNo: pageIndex,
    numOfRows: PAGE_SIZE,
    siDoCd,
    ...(siGunGuCd && { siGunGuCd }),
  });

  try {
    const xml = await fetchXml(`https://${API_HOST}${SEARCH_PATH}?${params}`);

    const resultCode = getTag(xml, 'resultCode');
    if (resultCode && resultCode !== '00' && resultCode !== '0000') {
      const msg = getTag(xml, 'resultMsg') || '알 수 없는 오류';
      return { statusCode: 502, headers, body: JSON.stringify({ error: `API 오류 [${resultCode}]: ${msg}` }) };
    }

    const result = parseSearchXml(xml);

    if (sigungu && !siGunGuCd && result.items.length > 0) {
      result.notice = `시군구 코드 미등록 (${sigungu}). 시도 전체 결과 표시 중.`;
    }

    return { statusCode: 200, headers, body: JSON.stringify(result) };
  } catch (err) {
    console.error('LTC API 오류:', err.message);
    return { statusCode: 502, headers, body: JSON.stringify({ error: '외부 API 호출 중 오류: ' + err.message }) };
  }
};

function parseSearchXml(xml) {
  const totalCount = parseInt(getTag(xml, 'totalCount') || '0', 10);
  const pageNo = parseInt(getTag(xml, 'pageNo') || '1', 10);
  const numOfRows = parseInt(getTag(xml, 'numOfRows') || '10', 10);

  const items = [];
  const itemRegex = /<item>([\s\S]*?)<\/item>/g;
  let match;
  while ((match = itemRegex.exec(xml)) !== null) {
    const b = match[1];
    const pttnCd = getTag(b, 'adminPttnCd');
    items.push({
      name: getTag(b, 'adminNm') || '',
      facilityType: facilityTypeName(pttnCd),
      facilityTypeCode: pttnCd,
      code: getTag(b, 'longTermAdminSym') || '',
      registDate: getTag(b, 'longTermPeribRgtDt') || '',
    });
  }

  return { totalCount, pageNo, numOfRows, items };
}

function getTag(xml, tag) {
  const m = xml.match(new RegExp(`<${tag}[^>]*>([\\s\\S]*?)<\\/${tag}>`));
  return m ? m[1].trim() : '';
}

function fetchXml(url) {
  return new Promise((resolve, reject) => {
    https.get(url, (res) => {
      const chunks = [];
      res.on('data', c => chunks.push(c));
      res.on('end', () => resolve(Buffer.concat(chunks).toString('utf-8')));
      res.on('error', reject);
    }).on('error', reject);
  });
}
